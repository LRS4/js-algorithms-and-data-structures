let quoteSample = "Blueberry 3.141592653s are delicious.";
let myRegex = /[h-s2-6]/ig; // i = ignore case (case insensitive) g = global (match all not just first)
let result = quoteSample.match(myRegex); // Change this line
